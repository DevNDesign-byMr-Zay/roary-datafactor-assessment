# Maintained service runtime
GOOGLE_CLOUD_PROJECT=assessment-project
VERTEX_LOCATION=us-central1
PORT=8080
LOG_LEVEL=info

# The frozen historical corpus under "Software Engineering & AI Tooling/" contains
# standalone examples with additional environment-variable names. Those examples
# are not read by the maintained service and intentionally do not widen this
# runtime configuration contract.
