name: release/github

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag matching package.json, for example v1.1.0"
        required: true
        type: string

permissions:
  contents: write

concurrency:
  group: github-release
  cancel-in-progress: false

jobs:
  release:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Check out main
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: npm

      - name: Reproducible install
        run: npm ci --ignore-scripts

      - name: Verify maintained release candidate
        run: |
          npm audit --audit-level=moderate
          npm run check
          docker compose config --quiet
          docker build --tag roary-release-candidate:${{ github.sha }} .

      - name: Build release evidence bundle
        shell: bash
        env:
          RELEASE_TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          node scripts/create-release-manifest.mjs "${{ inputs.tag }}" "$GITHUB_SHA" release-manifest.json
          npm sbom --sbom-format=cyclonedx > sbom.cdx.json
          printf '%s\n' "$GITHUB_SHA" > release-commit.txt
          sha256sum sbom.cdx.json release-commit.txt release-manifest.json > release-artifacts.sha256

      - name: Verify release evidence checksums
        run: sha256sum --check release-artifacts.sha256

      - name: Retain release evidence before publication
        uses: actions/upload-artifact@v7
        with:
          name: release-evidence-${{ inputs.tag }}
          if-no-files-found: error
          retention-days: 30
          path: |
            sbom.cdx.json
            release-commit.txt
            release-manifest.json
            release-artifacts.sha256

      - name: Validate requested tag
        shell: bash
        run: |
          set -euo pipefail
          version="$(node -p "require('./package.json').version")"
          expected="v${version}"
          if [ "${{ inputs.tag }}" != "$expected" ]; then
            echo "Requested tag must equal $expected" >&2
            exit 1
          fi
          if git rev-parse "${{ inputs.tag }}" >/dev/null 2>&1; then
            echo "Tag already exists: ${{ inputs.tag }}" >&2
            exit 1
          fi

      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${{ inputs.tag }}" \
            sbom.cdx.json release-commit.txt release-artifacts.sha256 release-manifest.json \
            --target "$GITHUB_SHA" \
            --title "${{ inputs.tag }}" \
            --generate-notes
