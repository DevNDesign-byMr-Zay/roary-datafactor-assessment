/* Aster JavaScript v192 — authenticated buyer-safe derivative: brush-size control normalization. Host state/dependencies are intentionally external. */
function brushSize(){ return Number($("#brushSize").value||64); }
