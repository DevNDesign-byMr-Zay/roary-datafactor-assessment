/* Aster JavaScript v195 — authenticated buyer-safe derivative: editor close-state cleanup. Host state/dependencies are intentionally external. */
function closeEditor(){ modal.classList.remove("show"); drawing=false; last=null; }
