/* Aster JavaScript v202 — authenticated buyer-safe derivative: feather-control normalization. Host state/dependencies are intentionally external. */
function feather(){ return Number($("#feather").value||0); }
