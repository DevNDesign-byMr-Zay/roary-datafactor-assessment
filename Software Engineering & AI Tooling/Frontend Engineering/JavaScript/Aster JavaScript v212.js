/* Aster JavaScript v212 — authenticated buyer-safe derivative: coordinated multi-surface render dispatcher. Host state/dependencies are intentionally external. */
function render(){
    renderChatsList();
    renderMediaList();
    renderCfg();
    renderChat();
  }
