/* Aster JavaScript v288 — authenticated buyer-safe derivative: bounded tool error logging. Host state/dependencies are intentionally external. */
function asterErr(){ try{ console.error.apply(console, arguments); }catch(e){} }
