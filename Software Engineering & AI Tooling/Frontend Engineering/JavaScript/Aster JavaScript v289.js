/* Aster JavaScript v289 — authenticated buyer-safe derivative: bounded tool warning logging. Host state/dependencies are intentionally external. */
function asterWarn(){ try{ console.warn.apply(console, arguments); }catch(e){} }
