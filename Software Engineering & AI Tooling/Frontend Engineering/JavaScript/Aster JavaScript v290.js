/* Aster JavaScript v290 — authenticated buyer-safe derivative: bounded tool logging. Host state/dependencies are intentionally external. */
function asterLog(){ try{ console.log.apply(console, arguments); }catch(e){} }
