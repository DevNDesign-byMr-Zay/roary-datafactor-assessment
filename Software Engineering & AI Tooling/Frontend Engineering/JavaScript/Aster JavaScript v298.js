/* Aster JavaScript v298 — authenticated buyer-safe derivative: mask-editor exit-state cleanup. Host state/dependencies are intentionally external. */
function exit(){
    // keep canvases around but hide by switching tool; or destroy if desired:
    // destroy();
  }
