/* Aster JavaScript v307 — authenticated buyer-safe derivative: active-image element resolution. Host state/dependencies are intentionally external. */
function getActiveImg(){return document.getElementById('imageModalImg');}
