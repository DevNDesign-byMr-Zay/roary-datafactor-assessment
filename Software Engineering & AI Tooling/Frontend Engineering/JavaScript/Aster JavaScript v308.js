/* Aster JavaScript v308 — authenticated buyer-safe derivative: active relight-panel resolution. Host state/dependencies are intentionally external. */
function getPanel(){return document.querySelector("#rtSidePanel");}
