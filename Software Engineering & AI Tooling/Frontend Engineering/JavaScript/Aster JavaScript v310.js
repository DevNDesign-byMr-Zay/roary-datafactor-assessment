/* Aster JavaScript v310 — authenticated buyer-safe derivative: monotonic operation timing. Host state/dependencies are intentionally external. */
function now(){ return (window.performance && performance.now) ? performance.now() : Date.now(); }
