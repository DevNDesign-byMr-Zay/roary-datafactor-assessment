/* Aster JavaScript v325 — authenticated buyer-safe derivative: panel visibility toggling. Host state/dependencies are intentionally external. */
function toggle(){
      if(slot.classList.contains('open')) close(); else open();
    }
